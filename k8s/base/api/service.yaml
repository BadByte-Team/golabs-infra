apiVersion: v1
kind: Service
metadata:
  name: golabs-api
  namespace: golabs
  labels:
    app.kubernetes.io/name: golabs-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: golabs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: golabs-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
