apiVersion: v1
kind: Service
metadata:
  name: golabs-ui
  namespace: golabs
  labels:
    app.kubernetes.io/name: golabs-ui
    app.kubernetes.io/component: ui
    app.kubernetes.io/part-of: golabs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: golabs-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
